#include<stdio.h>

void swap(int x, int y);

int main()
{
	int a = 3, b = 5;
	swap(a, b);
	printf("\n%d,%d\n", a, b);
	return 0;
}

void swap(int x, int y)
{
	int temp = 0;
	temp = x;
	x = y;
	y = temp;
	printf("swap함수 내 a,b는 %d,%d\n", x, y);
}